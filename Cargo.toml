# Workspace root. Build-out: the first product crate has landed — `charlie-ast`, the formula
# language contract surface (docs/architecture.md §5, W0). charlie-model and charlie-cli follow
# (W1+), at which point the `.annotated-tree.toml` deny edges + forbid_orphans are wired.
[workspace]
resolver = "2"
members = ["charlie-ast"]
