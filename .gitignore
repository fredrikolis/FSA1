# Rebuildable output only (repo-standards.md: never commit build output).
/target
**/*.rs.bk
Cargo.lock.orig

# The python venv used to AUTHOR the .ods fixtures is disposable — the generated fixtures
# (charlie-ingest/tests/fixtures/*.ods) ARE committed, so the Rust tests need no python/odfpy.
charlie-ingest/.fixture-venv/
**/__pycache__/
