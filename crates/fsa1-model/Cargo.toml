# Concern: declares the package and its one dep, fsa1-ast | Non-concern: the format libraries, which fsa1-xlsx and fsa1-ingest confine | IO: none
[package]
name = "fsa1-model"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
fsa1-ast = { path = "../fsa1-ast" }
