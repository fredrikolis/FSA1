# Concern: declares the package and the crates the verb layer composes | Non-concern: the deny edges barring the engine from depending on it (.annotated-tree.toml) | IO: none
[package]
name = "fsa1-verbs"
version.workspace = true
edition = "2024"
license.workspace = true

[dependencies]
fsa1-model = { path = "../fsa1-model" }
fsa1-html = { path = "../fsa1-html" }
fsa1-ingest = { path = "../fsa1-ingest" }
fsa1-xlsx = { path = "../fsa1-xlsx" }
comfy-table = "7.2"
annotated-tree = "0.6"
unicode-width = "0.2.2"
