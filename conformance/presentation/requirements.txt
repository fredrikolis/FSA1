# Pinned reference stack for the presentation round-trip oracle. This is a LOCK file: the full
# transitive closure is pinned so a rerun cannot silently pull a newer reader whose defaults flip a
# MATCH into a LOOK-DIV. `openpyxl` is the third-party reader this corpus grades the packed export
# through — FSA1 must never be its own visual oracle. The `formulas` half of the closure comes with
# the sibling `conformance/xl-oracle/oracle.py`, which presentation_oracle.py imports for the ONE
# definition of the fsa1-cli location and the parity-table clipper; duplicating those two here
# instead would be a second copy free to drift.
#
# Deliberately IDENTICAL to conformance/serde/requirements.txt and conformance/xl-oracle/
# requirements.txt, so all three corpora grade under one reference stack. Regenerate with
# `.venv/bin/pip freeze > requirements.txt` only when that shared reference is deliberately rev'd,
# and rev all three in the same change.
formulas==1.3.4
openpyxl==3.1.5
numpy==2.5.1
numpy-financial==1.0.0
scipy==1.18.0
schedula==1.6.15
click==8.4.2
et_xmlfile==2.0.0
python-dateutil==2.9.0.post0
regex==2026.7.10
six==1.17.0
tqdm==4.69.0
