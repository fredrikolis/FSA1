# Pinned reference stack for the ENG6 differential oracle (universal-testing-principles.md:
# "a verification oracle must be deterministic and reproducible"). This is a LOCK file: the full
# transitive closure is pinned so the reference `formulas` engine — and every dep whose semantics
# feed a computed reference value (numpy, scipy, numpy-financial, schedula) — is frozen. The
# documented reference (formulas v1.3.4, see README.md / PARITY.md) is thereby the ENFORCED
# reference; a rerun cannot silently pull a newer version whose semantics flip MATCH/DIVERGE.
# Regenerate with `.venv/bin/pip freeze > requirements.txt` only when the reference is deliberately
# rev'd (and update the version claims in README.md + PARITY.md in the same change).
formulas==1.3.4
openpyxl==3.1.5
numpy==2.5.1
numpy-financial==1.0.0
scipy==1.18.0
schedula==1.6.15
click==8.4.2
et_xmlfile==2.0.0
python-dateutil==2.9.0.post0
regex==2026.7.10
six==1.17.0
tqdm==4.69.0
